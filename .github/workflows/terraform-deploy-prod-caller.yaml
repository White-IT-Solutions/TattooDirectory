# .github/workflows/terraform-deploy-prod.yaml

name: 'Terraform CI/CD (prod)'

on:
  push:
    branches:
      - release-prod

jobs:
  deploy-infrastructure:
    uses: ./.github/workflows/terraform-reusable-deploy.yml
    with:
      environment: prod
      tf_state_key: prod/terraform.tfstate
      tf_var_file: auto.tfvars
      ssm_prefix: /tattoo-artist-directory-prod
      lambda_prefix: tattoo-artist-directory-prod
      kms_key_alias: alias/tattoo-artist-directory-prod-main
      apply_changes: true
    secrets:
      # Pass the AWS role from the 'prod' environment secrets
      AWS_ROLE_TO_ASSUME: ${{ secrets.AWS_ROLE_TO_ASSUME_PROD }}

  notify-deployment-started:
    uses: ./.github/workflows/deployment-notifications.yaml
    with:
      environment: prod
      deployment_status: started
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}

  integration-tests:
    needs: deploy-infrastructure
    uses: ./.github/workflows/integration-tests.yaml
    with:
      environment: prod
      api_url: "" # Will be retrieved from SSM
    secrets:
      AWS_ROLE_TO_ASSUME: ${{ secrets.AWS_ROLE_TO_ASSUME_PROD }}

  notify-deployment-result:
    needs: [deploy-infrastructure, integration-tests]
    if: always()
    uses: ./.github/workflows/deployment-notifications.yaml
    with:
      environment: prod
      deployment_status: ${{ needs.deploy-infrastructure.result == 'success' && needs.integration-tests.result == 'success' && 'success' || 'failure' }}
      deployment_url: "https://tattoo-directory.com" # Update with actual URL
      api_url: "https://api.tattoo-directory.com" # Update with actual URL
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}