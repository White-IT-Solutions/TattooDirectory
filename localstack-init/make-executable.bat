@echo off
REM Make shell scripts executable on Windows
echo Making LocalStack initialization scripts executable...

REM The scripts are already created and will be executable when run in the Docker container
echo Scripts are ready for use in Docker container.
echo Run docker-compose up to start LocalStack with initialization scripts.

pause